# The Tea Cozy

A Codecademy HTML/CSS exercise, built from the supplied design spec (`img-tea-cozy-redline.jpg`). Kept as a personal reference for the layout patterns — fixed header, full-bleed hero, flexbox grids.

`style.css` is commented throughout, explaining *why* rather than *what*.
